apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  namespace: clo835
spec:
  type: LoadBalancer
  selector:
    app: webapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  loadBalancerSourceRanges:
    - 0.0.0.0/0 